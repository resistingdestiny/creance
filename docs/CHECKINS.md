# ETHGlobal check-in drafts

Appended daily by the status job. Root pastes the current entry into the dashboard on check-in days.

## 2026-09-04

We started at 16:38 UTC today and merged six pull requests by the evening. The monorepo and CI are up. The Hedera testnet resources exist: the accounts, the tUSD settlement token, the policy NFT collection and the HCS topics, all recorded in docs/HEDERA.md. CoverPool and CollateralVault are deployed and verified as Sourcify exact matches, with the claim path run on testnet. The Scheduled Transactions spike measured the 62 day expiry window and gave us a premium helper. The demo note series ODI-COMP-2026-01 is issued through the Asset Tokenization Studio factory with KYC, pause, freeze and a declared coupon, and every step is linked to HashScan in docs/ATS.md. Each place where the documentation and the network disagreed is written up in docs/harness-notes.md.

Next is paying that coupon to the two noteholders through a Scheduled Transaction from the premium account, maturity redemption, and the investor endpoints. After that the API lane takes the quote, bind, policy NFT and HCS receipt core, then the x402 gating through Blocky402 so the Steward can make a real paid request. The web lane returns to the design system scaffold and then the World Selfie Check flow.

Two branches are stuck in review rather than blocked on a partner. The index model reproduces the backtest exactly, but its pushed head is an unfinished checkpoint and needs a rebase and a typecheck fix. The web scaffold passes every check and needs a rebase and one disclosure. Both are ours to fix and they gate the API and web lanes. We have no partner blockers so far, and we have not yet touched Blocky402, World or Bazantic.

## 2026-09-05

We merged twenty-two pull requests since yesterday's check-in and twenty-seven of our twenty-eight tickets are done. The API quotes, binds and mints the policy NFT, and its index and quote routes sit behind x402 with every settlement through the Blocky402 testnet facilitator and a receipt on the payments topic. The Steward agent completed a full paid cycle on testnet, from an index read to a bound policy and three premium schedules that executed. The oracle replayed the index onto its HCS topic and ran the live path over every occupation group. Two claims went end to end on testnet, one paid and one declined, with the Adjuster's decision records hashed onto the claims topic and a review queue for the human step. The worker screens, the claim screens and the investor desktop are built against the copy deck, the World Selfie Check runs at purchase and at claim with proofs verified server side, and the same web app now runs inside World App as a Mini App. We opened a pull request to the Hedera Harness with a mirror node reader and eighteen tests, published the index feed as its own agent readable gateway with a cold client that discovered it and paid on testnet, measured the clean clone run through at under four minutes, and shipped the demo seed, the nine shot list and a live reserve release.

Next is one open ticket, index operations: a scheduled daily check, revision records when the source moves, and a health route the deploy check reads. It is on its branch and needs a rebase before review. After that the build is done and the remaining work is ours as people: put the app on a public host at creance.co, make the repository public, create the Bazantic account and import the two gateways, register the Mini App, run the World Sandbox App pass on a phone, and record the main video, the showcase cut, the two Bazantic recordings and the harness clip.

We have no partner blockers. Everything that is not done needs a human rather than code: a VPS for the public host, a Bazantic provider account with its two business day response window, and a device with a face for the Selfie Check, since the staging simulator has no Selfie or Face credential. We ran the World flow end to end in staging with the Orb credential standing in and wrote every gap we met into the feedback document. The one thing we would ask a partner is whether World intends the simulator to gain a Selfie Check option, because a headless team cannot test that credential today.

## 2026-09-06

We merged six pull requests since yesterday's check-in and all thirty-three of our tickets are done. Index operations landed last night: a scheduled daily check, QA gates, revision records when the source moves, and a health route outside the paywall. Today was the public face of the product. The landing page is built from the design of record and reads its figures from the live feed, then brought up to the prototype with a dark hero, a cover card with real depth that follows the pointer, and a live occupation ticker. The home card ships in three directions behind one switch. The attribution panel sits beside the index and says what AI is doing to each occupation, as context only, never as settlement. The public index explorer is the page of record for the occupation displacement index, with sixty months of readings bought once per interval and a scrubber over them. The app is now on a public host: creance.co serves the web app and api.creance.co serves the API, with the health route reporting every dependency ok, the free index catalogue and the paid readings answering with a 402 exactly as they do on testnet.

Next is nothing that needs code. The remaining work is ours as people: apply for the Bazantic provider account and import the two gateways, make the repository public, register the Mini App in the World Developer Portal, run the Sandbox App pass on a phone against the live host, and record the main video, the showcase cut, the two Bazantic recordings and the harness clip. Small tidy up work waits on the lanes if we open tickets for it: pointing the generated API spec at the API's actual origin, baking the commit into the deployed image so the uptime check passes, and writing the deployment as it really is into the docs.

We have no partner blockers. The Bazantic account has a two business day response window and we have not yet applied, which is the one clock we are watching. The World Selfie Check still cannot be exercised headless because the staging simulator offers no Selfie or Face credential, so the device run waits on a phone and a face; the live host at least means it needs no tunnel. Our harness pull request to hedera-dev is open with no review yet, which is expected over a weekend and blocks nothing on our side.

## 2026-09-10

We merged eight pull requests today and closed seven tickets, on 113 small commits. Cover now exists for all fifteen occupation groups instead of one: each group has its own series on Hedera testnet, opened, registered, funded and issued its own Displacement Bond Note through the Asset Tokenization Studio, and the picker offers fifteen buyable occupations. The front door stopped costing an on-chain settlement per page view, because the landing figures are now streamed out of a held read rather than bought for every visitor, and the payments underneath are still real. A person who buys cover can get back into it, by World ID or by a twenty character cover key issued once at bind and stored only as a digest, and the receipt became a dashboard with a status line, its index chart and its history. The payment step now offers a choice between the demo wallet and the buyer's own, connect only for this phase and the documentation says so. A World check that is refused says which kind of credential it refused, at purchase and at claim, and logs the reason without ever naming the person. The landing page was measured at 886 words and cut back by deleting the answers the page already gave elsewhere.

Next are the two tickets in flight. One publishes a single link that reaches a real testnet cover with no World ID, no wallet and no purchase, so a judge can see the dashboard and the on chain figures in under a minute, with anything that is a fixture saying so on its own face. The other makes the web origin answer the three description files the index feed's documentation promises, which are served correctly on the API origin and were not routed on the public one. After those, the investor side gets several real coupon periods so the history reads as an accrual rather than a single payment, a total earned to date, and a design pass inside the existing tokens. Then we bring the shot list up to the product, because the purchase became one page and the list still describes five navigations.

We have no partner blockers, and the one clock we do not hold is now tight. We have still not applied for the Bazantic provider account and the response window is two business days, so applying tonight means a reply on Monday at the earliest and one day to build two gateways and record two screens. Our pull request to the Hedera Harness was reviewed by a hedera-dev collaborator on 8 September, who accepted the contribution and asked for three small changes, and we have not yet answered it. The World Selfie Check still cannot be exercised without a device, because the staging simulator returns an orb credential to a Selfie Check deployment and offers no Selfie or Face option at all, which we confirmed again today while building the refusal messages. The rest of what is open needs a redeploy of our public host rather than more code.

## 2026-09-11

We merged twelve pull requests since yesterday's check-in and closed twelve tickets, on 130 small commits, and every ticket on our board is now done. The investor side shows money accumulating: two more coupon periods were declared on the demo note and settled on Hedera testnet through Scheduled Transactions, so the history reads as three months and six receipts rather than one date, with earned to date and the next payment on a certificate drawn in the same metal as the cover card. The product became one product, with one header and one footer on every route, one ground under the three frames and the investor page streaming its chain reads out of a held read instead of paying for them on every view. The site is findable, with a title, a description and a canonical on every public page, Open Graph cards, a sitemap and structured data, and the hero now shows the product working, ringed by chips built only from records the page already reads, each linking to HashScan or to the explorer. We also fixed two honesty problems: the index documentation no longer claims the source data is never revised, and the buyer is told how close the newest reading came to the line and which five occupations have a level line that has never been reached. The public host was rebuilt today and creance.co serves all of it, including the three description files an agent needs, and a real published cover a judge can open with no World ID, no wallet and no purchase.

Next is nothing that needs a ticket on the board, and the work that remains is ours as people. We answer the review on our Hedera Harness pull request, where a maintainer accepted the contribution on 8 September and asked for three small changes. We bring the shot list up to the product, because the purchase became one page and the list still describes five navigations, and then record the main video, the showcase cut and the harness clip against the live host. We put the World feedback document back under docs in the public repository, register the Mini App, run the Sandbox App pass on a phone, and finish the host with the running commit baked into the health route and the uptime check pointed at it. If we open more tickets, they are small: reconcile the published months' receipts so the index feed says which topic message each reading came from, re-measure the clean clone run through, and write the deployment into the docs as it really is.

We have no partner blockers, and the one clock we do not hold has run out on its ordinary path. We never applied for the Bazantic provider account, the response window is two business days, and two business days from today is the submission day, so we will apply tonight and write the fallback into the form: the recipes are committed and runnable but were never run through a gateway, and our Agentify entry leads with the cold client transcript, which needs no gateway. The World Selfie Check still cannot be exercised without a device, because the staging simulator offers no Selfie or Face credential and returns an orb credential to a Selfie Check deployment. One thing we found today is outside our repository: Cloudflare prepends a managed robots file to ours that disallows the crawlers that feed models, so a site we built to be read by a model is blocked at the edge until we change the zone setting.

## 2026-09-18

Our last check-in was on 11 September, and since then we shipped two days of work and then nothing. On 12 and 13 September we added a secondary market for the notes on Hedera testnet, a no custody venue where a holder offers a lot at a price and a buyer takes it in one transaction carrying both legs, with the note's own KYC gate proved twice by a transfer refused and then settled with only the KYC record changed between. We built a market board on the investor page with a rate history per occupation and a Take button the note itself refuses, an activity page that lists every action this product has taken on chain, a premium that is solved from the return capital requires so an open month is refused a quote, three real covers on testnet behind one link a judge can open with no World ID, no wallet and no purchase, a subscribe route so the investor screen's button actually sends, and a landing section that shows when the cover pays as a sum with graphics. We rewrote the README around how the product works and moved the operational sections into a separate internals document, redeployed the public host on 13 September, and rewrote the shot list against the live product. The host has been up since with every dependency reporting ok, and the Cloudflare robots file that blocked model crawlers is gone. There have been no commits since 13 September.

Next is two regressions we found tonight and the same human steps as last week. The public index explorer at creance.co/index, which is the page of record for the occupation displacement index and is in our sitemap, hangs or answers 500 on the host while every other route answers in under a quarter of a second, so it needs the container log read and a fix or a rollback. The README rewrite removed the setup section, the tokenization steps and the measured clean clone run through that three of our prize lines cite, so we either put those sections back or point the lines at the internals document. After that, the list is what it was: answer the maintainer's review on our Hedera Harness pull request, put the World feedback document back under docs in the public repository, bake the running commit into the health route, and run the oracle on the host, because the newest published month is now 48 days old against a 45 day staleness threshold and the health route says so.

We have no partner blockers, and the deadline has passed. The event ran to 16 September and the repository holds no video link, no Bazantic username and no feedback document at the path the World track names, and whether the submission form was filed is not something we can see from the repository. The Bazantic account was never applied for, so the recipes are committed and runnable but were never run through a gateway, and our Agentify entry rests on the cold client transcript that needs no gateway. The World Selfie Check still cannot be exercised without a device, because the staging simulator offers no Selfie or Face credential. The one thing we would still ask a partner is the same as before: whether World intends the simulator to gain a Selfie Check option, because a headless team cannot test that credential today.

## 2026-09-21

Since our last check-in on 18 September we found and fixed the outage behind the public index explorer, and it was not our page. The Blocky402 testnet facilitator our x402 gate settled through stopped putting transactions on chain on 15 September while still answering every probe in under half a second, so each settlement hung to the Hedera SDK's thirty second timeout and the explorer, which buys fifteen readings a render, answered 500 for five days. The x402 protocol does not require the facilitator to be somebody else's server, so the API now runs the Hedera facilitator scheme in process with our own operator as fee payer, and the first paid read after the fix settled on chain in under two seconds where it had taken thirty and failed. With the fee now ours we stopped metering the index reading, because a public page buying fifteen readings every ten minutes was about thirty HBAR a day of our own money; quote and bind stay paid. We also turned off two demo credential issuers that had been reachable on the public internet, put the oracle pipeline on firmer ground so a newly published month reaches the database without a redeploy, and started the inventory and configuration work for running the contracts on an EVM chain. That work lives in a private continuation of this repository, because on 19 September we decided to take the product past the hackathon on a regular chain; the public repository is frozen at 13 September and creance.co runs the continuation.

Next is a decision more than a build. The hackathon submission package is in the same state as on 18 September: no video link, no Bazantic account, and the World feedback document still not at the path the track names, and with the product moving on we have to say whether that package is still something we are finishing. Two things are owed regardless. A maintainer on the Hedera Harness accepted our pull request on 8 September and asked for three small changes, and we have not answered in thirteen days. And Blocky402 should hear from us that its testnet facilitator has been silent since 15 September, because the next team to build on it will lose the time we lost. After that, if the submission stands, the list is the recordings, the Bazantic gateway, the feedback document, and a README that says plainly which code the live site runs.

We have no partner blockers, and one partner failure worth reporting: api.testnet.blocky402.com has submitted nothing to Hedera testnet since 16:59 UTC on 15 September, and the mirror node confirms it, while its verify and settle endpoints keep answering as if nothing were wrong. We routed around it rather than waiting. The World Selfie Check still cannot be exercised without a device, because the staging simulator offers no Selfie or Face credential. The index is 51 days stale against a 45 day threshold because no scheduled oracle run has happened on the host, which is ours to run.
