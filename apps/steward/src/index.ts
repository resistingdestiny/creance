export const workspaceName = '@creance/steward';

export const summary = 'Buyer agent: x402 client, decision rule and premium scheduling';

/** One line describing what this workspace is for. */
export function describeWorkspace(): string {
  return `${workspaceName}: ${summary}`;
}
