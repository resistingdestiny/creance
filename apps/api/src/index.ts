export const workspaceName = '@creance/api';

export const summary = 'Fastify API: quotes, binding, claims, x402 middleware and World verification';

/** One line describing what this workspace is for. */
export function describeWorkspace(): string {
  return `${workspaceName}: ${summary}`;
}
