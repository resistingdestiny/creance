node_modules/
.env
.env.*
!.env.example
.autopilot/
.claude/settings.local.json
.claude/worktrees/
dist/
.next/
*.tsbuildinfo
next-env.d.ts
coverage/
artifacts/
cache/

# Everything a run writes at runtime: the raw BLS cache under var/cache/bls, the
# oracle's run state and observation store under var/oracle, and what the Steward
# remembers between runs under var/steward. Derived data, never committed; the
# archive that is committed lives in data/bls.
var/
typechain-types/
*.log
.DS_Store

# Browser session dumps written by the Playwright tooling during a review.
.playwright-mcp/

# Hardhat 3 typechain output, regenerated on every build.
contracts/types/

# Scratch files a builder leaves inside a package while working something out.
.scratch/

docs/PRIZES.md
docs/FEEDBACK-WORLD.md
docs/ARCHITECTURE.md
docs/PRICING.md
