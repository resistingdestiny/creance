{
  "name": "@creance/oracle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "once": "tsx --env-file-if-exists=../../.env src/cli/once.ts",
    "replay": "tsx --env-file-if-exists=../../.env src/cli/replay.ts",
    "preflight": "tsx --env-file-if-exists=../../.env src/cli/preflight.ts",
    "schedule": "tsx --env-file-if-exists=../../.env src/cli/schedule.ts",
    "verify": "tsx --env-file-if-exists=../../.env src/cli/verify.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "@creance/client": "workspace:*",
    "@creance/index-model": "workspace:*",
    "@hiero-ledger/sdk": "2.87.0",
    "ethers": "^6.15.0"
  }
}
